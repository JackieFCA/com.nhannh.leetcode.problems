package problems.easy.twosum;

public class TwoSum {

    public int[] twoSum(int[] nums, int target) {
        // validation step
        if (nums.length < 2 || nums.length > Math.pow(10, 4) || Math.pow(10, -9) < target || Math.pow(10, 9) > target) {
            return null;
        }
        return null;
    }
}